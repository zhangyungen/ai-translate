## 1. Implementation
- [x] 1.1 初始化插件工程（TypeScript + MV3 + background/content/popup 基础结构）。
- [x] 1.2 建立领域模型、应用服务、基础设施网关与统一常量/枚举模块。
- [x] 1.3 实现腾讯接口网关（统一 `/api/imt` 调用、`client_key` 注入、错误映射、warn 日志）。
- [x] 1.4 实现语言列表缓存与自动续期（TTL=24h）。
- [x] 1.5 实现 popup 交互（语言选择、互换、输入翻译、字数限制、入口按钮）。
- [x] 1.6 实现整页翻译开关与回滚（保留原文映射，支持开关切换）。
- [x] 1.6.1 实现视口增量翻译（滚动到哪里翻译到哪里，支持滚动/resize/DOM 变更触发）。
- [x] 1.6.2 实现翻译结果缓存（内存 + 本地持久化，命中优先，带容量控制）。
- [x] 1.6.3 实现模式互斥状态机（阅读模式与整页翻译不可同时开启，切换需先关闭旧模式再翻译新模式）。
- [x] 1.6.4 实现页面级自动语言决策（来源自动识别；目标=中文->英文、英文->中文、否则浏览器默认语言；同语种提示无需翻译）。
- [x] 1.6.5 实现整页翻译重复展示抑制（候选键去重 + 连续相同译文跳过）。
- [x] 1.6.6 实现页眉/页脚候选补强（`header/nav/footer` 与语义 role 容器可见文本叶子节点纳入翻译候选）。
- [x] 1.7 实现阅读模式（增强规则断句分段，分隔符 `  |  `，展示顺序固定为“断句 -> 生僻词 -> 整段翻译”，按类型去重 + 术语行内去重，悬停 0.3s 与选中翻译）。
- [x] 1.7.1 区分阅读模式断句块与整段翻译块背景色。
- [x] 1.7.2 阅读模式中少于 3 个单词的文本不展示断句分割结果。
- [x] 1.8 完成样式与交互对齐（参考腾讯图示，兼容桌面/移动视口）。
- [x] 1.8.1 在 popup 展示当前模式（无/整页翻译/阅读模式），并在切换后实时刷新。
- [x] 1.8.2 手动点击 popup 模式开关时，按 popup 当前源语言/目标语言执行翻译。

## 2. Validation
- [x] 2.1 增加单元测试（分句器、key provider、缓存续期、请求组装）。
- [x] 2.2 增加集成测试（消息通道、整页翻译切换、视口增量翻译、模式互斥切换、hover 翻译触发）。
- [x] 2.3 在 `https://www.nytimes.com/` 进行手工验证并记录检查项。

## 3. Delivery
- [x] 3.1 提供一键打包命令（如 `npm run build:zip`）并产出可加载扩展包。
- [x] 3.2 补充 README（安装、权限、使用方式、已知限制与日志说明）。
